import { Injector, ComponentFactoryResolver, ViewContainerRef, OnDestroy } from '@angular/core';
import { NodeComponent } from './node.component';
import { Observable } from 'rxjs';
export declare class CreateFlowService implements OnDestroy {
    private injector;
    private resolver;
    private nodeList;
    private nodeFactory;
    firstOnTop: boolean;
    private finishedSubscription;
    private headingChangedSubscription;
    private overFlowSubscriptions;
    private nodesSubject;
    nodes: Observable<NodeComponent[]>;
    constructor(injector: Injector, resolver: ComponentFactoryResolver);
    clearNodes(): void;
    createFlow(textPassage: string, location: ViewContainerRef, style: any, showPageNumbers: boolean, isOverlaid: boolean): void;
    private createNode(content, location?, style?, showPageNumbers?, isOverlaid?);
    ngOnDestroy(): void;
}
